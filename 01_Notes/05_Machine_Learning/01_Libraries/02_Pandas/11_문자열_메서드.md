<h1>Pandas 학습 가이드: 데이터 과학자를 위한 실무 중심 로드맵</h1>
작성자: Alpine_Dolce&nbsp;&nbsp;|&nbsp;&nbsp;날짜: 2025-07-01 (최종 수정: 2025-08-13)

<h2>문서 목표</h2>
이 문서는 Pandas Series의 `.str` 접근자를 활용하여 문자열 데이터를 효율적으로 처리하는 방법을 다룹니다. 문자열 메서드의 기본 사용법, 정규 표현식(Regular Expression)과의 연동, 그리고 일반적인 문자열 처리 작업(분리, 대체, 추출 등)을 실제 코드 예제를 통해 학습합니다.

<h2>목차</h2>

- [목차](#목차)
- [1. `.str` 접근자란?](#1-str-접근자란)
- [2. 기본 문자열 메서드](#2-기본-문자열-메서드)
  - [2.1. 대소문자 변환](#21-대소문자-변환)
  - [2.2. 공백 및 특정 문자 제거](#22-공백-및-특정-문자-제거)
  - [2.3. 문자열 길이](#23-문자열-길이)
- [3. 문자열 분리 및 결합](#3-문자열-분리-및-결합)
  - [3.1. `split()` / `rsplit()`: 문자열 분리](#31-split--rsplit-문자열-분리)
  - [3.2. `join()`: 문자열 결합](#32-join-문자열-결합)
- [4. 문자열 대체 및 추출 (정규 표현식 활용)](#4-문자열-대체-및-추출-정규-표현식-활용)
  - [4.1. `replace()`: 문자열 대체](#41-replace-문자열-대체)
  - [4.2. `extract()`: 정규 표현식으로 패턴 추출](#42-extract-정규-표현식으로-패턴-추출)
  - [4.3. `findall()`: 모든 매칭 패턴 찾기](#43-findall-모든-매칭-패턴-찾기)
- [5. 기타 유용한 문자열 메서드](#5-기타-유용한-문자열-메서드)
  - [5.1. 접두사/접미사 확인 (`startswith()`, `endswith()`)](#51-접두사접미사-확인-startswith-endswith)

--- 

## 1. `.str` 접근자란?

Pandas `Series`나 `Index`의 `dtype`이 `object`(주로 문자열)일 때, `.str` 접근자를 통해 문자열 전용 메서드들을 사용할 수 있습니다. 이 접근자는 파이썬의 내장 문자열 메서드와 유사한 기능을 제공하며, `Series`의 모든 요소에 **벡터화된(vectorized) 방식**으로 적용되어 효율적인 문자열 처리를 가능하게 합니다.

**왜 `.str` 접근자를 사용해야 하는가?**
*   **벡터화된 연산**: 파이썬의 `for` 루프를 사용하는 것보다 훨씬 빠르고 효율적입니다. `Series`의 모든 문자열 요소에 한 번에 연산을 적용할 수 있어 대규모 데이터 처리 시 성능 이점을 제공합니다.
    ```python
    import pandas as pd
    import time

    s_py = ['hello world'] * 100_000 # 파이썬 리스트
    s_pd = pd.Series(s_py) # Pandas Series

    # 파이썬 for 루프
    start = time.time()
    result_py = [x.upper() for x in s_py]
    end = time.time()
    print(f"파이썬 for 루프 실행 시간: {end - start:.4f} 초")

    # Pandas .str 접근자
    start = time.time()
    result_pd = s_pd.str.upper()
    end = time.time()
    print(f"Pandas .str.upper() 실행 시간: {end - start:.4f} 초")
    ```
*   **결측치(`NaN`) 처리**: 문자열이 아닌 값(예: `NaN`)이 포함되어 있어도 오류 없이 처리됩니다. `NaN`이 포함된 요소에 대해서는 결과도 `NaN`으로 반환됩니다.
    ```python
    import pandas as pd
    import numpy as np

    s_nan = pd.Series(['apple', np.nan, 'banana'])
    print(f"원본 Series (NaN 포함):
{s_nan}")
    print(f"대문자 변환 결과:
{s_nan.str.upper()}")
    ```
*   **간결한 코드**: 복잡한 루프나 `apply()` 함수 없이도 간결하게 문자열 처리를 수행할 수 있어 코드의 가독성과 유지보수성을 높입니다.

## 2. 기본 문자열 메서드

`.str` 접근자를 통해 사용할 수 있는 기본적인 문자열 메서드들은 파이썬의 내장 문자열 메서드와 유사합니다.

### 2.1. 대소문자 변환

`lower()`, `upper()`, `capitalize()`, `title()`, `swapcase()`, `casefold()` 등을 사용하여 문자열의 대소문자를 변경합니다.

```python
import pandas as pd

s = pd.Series(['apple', 'Banana', 'ORANGE', 'grape', 'Straße'])
print(f"원본 Series:
{s}")

print(f"\n소문자 변환 (lower()):
{s.str.lower()}")
print(f"대문자 변환 (upper()):
{s.str.upper()}")
print(f"첫 글자만 대문자 (capitalize()):
{s.str.capitalize()}")
print(f"각 단어 첫 글자 대문자 (title()):
{s.str.title()}")
print(f"대소문자 반전 (swapcase()):
{s.str.swapcase()}")
print(f"대소문자 구분 없는 비교를 위한 변환 (casefold()):
{s.str.casefold()}") # 'Straße' -> 'strasse'
```

### 2.2. 공백 및 특정 문자 제거

`strip()`, `lstrip()`, `rstrip()`을 사용하여 문자열 양쪽, 왼쪽, 오른쪽의 공백 또는 지정된 문자를 제거합니다.

```python
import pandas as pd

s = pd.Series(['  apple  ', 'Banana ', ' ORANGE ', '---test---'])
print(f"원본 Series:
{s}")

print(f"\n양쪽 공백 제거 (strip()):
{s.str.strip()}")
print(f"왼쪽 공백 제거 (lstrip()):
{s.str.lstrip()}")
print(f"오른쪽 공백 제거 (rstrip()):
{s.str.rstrip()}")

# 특정 문자 제거
print(f"\n양쪽에서 '-' 제거 (strip('-')):
{s.str.strip('-')}")
```

### 2.3. 문자열 길이

`len()` 메서드를 사용하여 각 문자열 요소의 길이를 반환합니다.

```python
import pandas as pd

s = pd.Series(['apple', 'banana', 'orange', '데이터 과학'])
print(f"원본 Series:
{s}")

print(f"\n문자열 길이 (len()):
{s.str.len()}")
```

## 3. 문자열 분리 및 결합

문자열 데이터를 특정 구분자를 기준으로 분리하거나, 여러 문자열을 하나로 결합하는 기능은 데이터 전처리에서 매우 중요합니다.

### 3.1. `split()` / `rsplit()`: 문자열 분리

`split(pat=None, n=-1, expand=False)` 메서드는 문자열을 구분자(`pat`)를 기준으로 분리합니다. `expand=True`로 설정하면 분리된 문자열이 새로운 `DataFrame`의 컬럼으로 확장됩니다. `rsplit()`은 오른쪽에서부터 분리를 시작합니다.

```python
import pandas as pd

s = pd.Series(['apple,banana,cherry', 'date,elderberry', 'fig'])
print(f"원본 Series:
{s}")

# 쉼표로 분리
print(f"\n쉼표로 분리 (split(',')):
{s.str.split(',')}")

# 쉼표로 분리하고 DataFrame으로 확장
print(f"\n쉼표로 분리 (expand=True):
{s.str.split(',', expand=True)}")

# 첫 번째 쉼표만 기준으로 분리
print(f"\n첫 번째 쉼표로 분리 (n=1):
{s.str.split(',', n=1, expand=True)}")

# 오른쪽에서부터 분리 (rsplit)
s_path = pd.Series(['/home/user/doc.txt', '/var/log/syslog.log'])
print(f"\n원본 경로 Series:
{s_path}")
print(f"오른쪽에서 첫 번째 '/'로 분리 (rsplit('/', n=1)):
{s_path.str.rsplit('/', n=1, expand=True)}")
```

### 3.2. `join()`: 문자열 결합

`join(sep)` 메서드는 `Series`의 문자열 요소들(리스트 또는 튜플 형태)을 `sep` 구분자로 결합합니다. 이는 `split()`의 역방향 연산으로 볼 수 있습니다.

```python
import pandas as pd

s = pd.Series([['apple', 'banana'], ['date', 'elderberry'], ['fig']])
print(f"원본 Series (리스트 포함):
{s}")

# 리스트 내부의 문자열을 쉼표로 결합
print(f"\n쉼표로 결합 (join(',')):
{s.str.join(',')}")

# 공백으로 결합
print(f"\n공백으로 결합 (join(' ')):
{s.str.join(' ')}")
```

## 4. 문자열 대체 및 추출 (정규 표현식 활용)

`.str` 접근자는 정규 표현식(Regular Expression)을 지원하여 복잡한 패턴 매칭 및 조작을 가능하게 합니다.

### 4.1. `replace()`: 문자열 대체

`replace(pat, repl, regex=True)` 메서드는 특정 패턴(`pat`)을 다른 문자열(`repl`)로 대체합니다. `regex=True`로 설정하면 `pat`에 정규 표현식을 사용할 수 있습니다. `repl`에 함수를 전달하여 동적으로 대체 값을 생성할 수도 있습니다.

```python
import pandas as pd

s = pd.Series(['apple_pie', 'banana_split', 'orange_juice', '123-456-7890'])
print(f"원본 Series:
{s}")

# '_'를 공백으로 대체
print(f"\n'_'를 공백으로 대체 (replace('_', ' ')):
{s.str.replace('_', ' ')}")

# 정규 표현식을 이용한 대체: 모음 제거
print(f"\n모음 제거 (replace(r'[aeiou]', '')):
{s.str.replace(r'[aeiou]', '', regex=True)}")

# 함수를 이용한 대체: 전화번호 중간 4자리 마스킹
print(f"\n전화번호 중간 4자리 마스킹 (함수 사용):
{s.str.replace(r'(\d{3})-(\d{4})-(\d{4})', lambda m: f''{m.group(1)}-****-{m.group(3)}'', regex=True)}")
```

### 4.2. `extract()`: 정규 표현식으로 패턴 추출

`extract(pat)` 메서드는 정규 표현식의 캡처 그룹(괄호로 묶인 부분)을 사용하여 문자열에서 특정 패턴을 추출하고, 이를 새로운 `DataFrame`의 컬럼으로 반환합니다.

```python
import pandas as pd

s = pd.Series(['Name: Alice, Age: 30', 'Name: Bob, Age: 25', 'Name: Charlie'])
print(f"원본 Series:
{s}")

# 이름과 나이 추출
pattern = r'Name: (.*), Age: (\d+)'
extracted_data = s.str.extract(pattern)
print(f"\n추출된 데이터 (extract()):
{extracted_data}")

# expand=False 예시 (Series of tuples 반환)
extracted_data_no_expand = s.str.extract(pattern, expand=False)
print(f"\n추출된 데이터 (extract(expand=False)):
{extracted_data_no_expand}")
```

### 4.3. `findall()`: 모든 매칭 패턴 찾기

`findall(pat)` 메서드는 정규 표현식(`pat`)과 일치하는 모든 비-겹치는(non-overlapping) 패턴을 찾아 리스트 형태로 반환합니다.

```python
import pandas as pd

s = pd.Series(['apple123banana456', 'orange789', 'no_numbers_here'])
print(f"원본 Series:
{s}")

# 모든 숫자 시퀀스 찾기
print(f"\n모든 숫자 시퀀스 (findall(r'\d+')):
{s.str.findall(r'\d+')}")

# 모든 모음 찾기
print(f"\n모든 모음 (findall(r'[aeiou]')):
{s.str.findall(r'[aeiou]')}")
```

## 5. 기타 유용한 문자열 메서드

`.str` 접근자는 이 외에도 다양한 유용한 문자열 메서드를 제공합니다.

### 5.1. 접두사/접미사 확인 (`startswith()`, `endswith()`)

문자열이 특정 접두사나 접미사로 시작하거나 끝나는지 확인합니다.

```python
import pandas as pd

s = pd.Series(['apple', 'banana', 'apricot', 'grape'])
print(f"원본 Series:
{s}")

print(f"\n'ap'로 시작하는지 (startswith('ap')):
{s.str.startswith('ap')}")
print(f