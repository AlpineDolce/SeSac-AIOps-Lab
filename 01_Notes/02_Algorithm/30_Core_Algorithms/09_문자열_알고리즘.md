<h2>알고리즘과 자료구조 마스터하기: 문자열 알고리즘</h2>
작성자: Alpine_Dolce&nbsp;&nbsp;|&nbsp;&nbsp;날짜: 2025-08-07 (최종 수정: 2025-08-11)

<h2>문서 목표</h2>
<p>이 문서는 컴퓨터 과학의 핵심인 알고리즘과 자료구조에 대한 포괄적인 학습 가이드의 아홉 번째 부분으로, 문자열 데이터를 효율적으로 처리하고 분석하는 다양한 알고리즘을 다룹니다. 문자열 매칭, 접미사 배열, 문자열 압축 등 실용적인 문자열 문제 해결 능력을 강화하는 것을 목표로 합니다.</p>

<h2>목차</h2>

- [1. 문자열 매칭 (String Matching)](#1-문자열-매칭-string-matching)
  - [1.1. 나이브(Naive) 문자열 매칭](#11-나이브naive-문자열-매칭)
  - [1.2. KMP (Knuth-Morris-Pratt) 알고리즘](#12-kmp-knuth-morris-pratt-알고리즘)
  - [1.3. 라빈-카프 (Rabin-Karp) 알고리즘](#13-라빈-카프-rabin-karp-알고리즘)
  - [1.4. 보이어-무어 (Boyer-Moore) 알고리즘](#14-보이어-무어-boyer-moore-알고리즘)
  - [1.5. 문자열 매칭 알고리즘 비교](#15-문자열-매칭-알고리즘-비교)
- [2. 접미사 배열 (Suffix Array) 및 LCP 배열](#2-접미사-배열-suffix-array-및-lcp-배열)
  - [2.1. 접미사 배열 (Suffix Array)](#21-접미사-배열-suffix-array)
  - [2.2. LCP 배열 (Longest Common Prefix Array)](#22-lcp-배열-longest-common-prefix-array)
  - [2.3. 접미사 배열과 LCP 배열의 구현 및 활용](#23-접미사-배열과-lcp-배열의-구현-및-활용)
- [3. 문자열 압축](#3-문자열-압축)
  - [3.1. 런 길이 인코딩 (Run-Length Encoding, RLE)](#31-런-길이-인코딩-run-length-encoding-rle)
  - [3.2. 허프만 코딩 (Huffman Coding)](#32-허프만-코딩-huffman-coding)
  - [3.3. 다른 문자열 압축 알고리즘](#33-다른-문자열-압축-알고리즘)
  - [3.4. 문자열 압축의 활용](#34-문자열-압축의-활용)

---

## 1. 문자열 매칭 (String Matching)

**문자열 알고리즘**
문자열 알고리즘은 텍스트 데이터에서 특정 패턴을 찾거나, 문자열을 효율적으로 처리하는 데 사용되는 알고리즘입니다. 텍스트 편집기, 검색 엔진, 생물 정보학, 데이터 압축 등 다양한 분야에서 핵심적인 역할을 합니다. 이 섹션에서는 문자열 매칭, 접미사 배열, 문자열 압축 등 주요 문자열 알고리즘의 개념과 구현을 다룹니다.

문자열 매칭은 긴 텍스트(Text) `T` 내에서 특정 패턴(Pattern) `P`가 나타나는 모든 시작 위치를 찾는 문제입니다. 이는 텍스트 편집기, 검색 엔진, 생물 정보학 등 다양한 분야에서 핵심적인 역할을 합니다.

### 1.1. 나이브(Naive) 문자열 매칭

가장 간단한 문자열 매칭 알고리즘입니다. 텍스트의 모든 가능한 위치에서 패턴과 직접 비교합니다.

*   **동작 방식**: 텍스트의 첫 번째 문자부터 시작하여 패턴의 길이만큼 텍스트의 부분 문자열을 패턴과 비교합니다. 일치하지 않으면 텍스트에서 한 칸 옆으로 이동하여 다시 비교합니다.
*   **시간 복잡도**: 최악의 경우 `O((N-M+1)M)` 또는 `O(NM)` (N: 텍스트 길이, M: 패턴 길이). 예를 들어, "AAAAA"에서 "AAA"를 찾는 경우.
*   **장점**: 구현이 매우 간단합니다.
*   **단점**: 비효율적이며, 불필요한 비교가 많습니다.

```python
def naive_search(text, pattern):
    """나이브 문자열 매칭 알고리즘"""
    N = len(text)
    M = len(pattern)
    occurrences = []
    for i in range(N - M + 1):
        if text[i:i+M] == pattern:
            occurrences.append(i)
    return occurrences

text_naive = "ABAAABCD"
pattern_naive = "ABC"
print(f"나이브 검색 결과 (텍스트: {text_naive}, 패턴: {pattern_naive}): {naive_search(text_naive, pattern_naive)}")
# 예상 출력: [4]
print("
")
```

### 1.2. KMP (Knuth-Morris-Pratt) 알고리즘

KMP 알고리즘은 나이브 매칭의 비효율성을 개선한 알고리즘입니다. 패턴 자체의 구조(접두사와 접미사의 일치)를 이용하여 불필요한 비교를 건너뛰어 효율성을 높입니다.

*   **핵심 아이디어**: 패턴에서 불일치가 발생했을 때, 패턴을 단순히 한 칸 이동시키는 대신, 이미 비교했던 패턴의 부분 문자열 정보를 활용하여 패턴을 최대한 많이 이동시킵니다. 이를 위해 **부분 일치 테이블(Partial Match Table)** 또는 **LPS(Longest Proper Prefix which is also Suffix) 배열**을 미리 계산합니다.
    *   **LPS 배열**: `lps[i]`는 패턴의 `0`부터 `i`까지의 부분 문자열에서, 가장 긴 '진접두사(Proper Prefix)'가 '진접미사(Proper Suffix)'와 일치하는 길이입니다.
*   **동작 방식**:
    1.  **전처리 단계**: 패턴 `P`에 대한 LPS 배열을 `O(M)` 시간에 계산합니다.
    2.  **매칭 단계**: 텍스트 `T`와 패턴 `P`를 비교합니다. 불일치가 발생하면, LPS 배열을 참조하여 패턴을 이동시킵니다. `j`가 불일치가 발생한 패턴의 인덱스라면, 패턴을 `j - lps[j-1]`만큼 이동시킵니다.
*   **시간 복잡도**: `O(N + M)` (전처리 `O(M)`, 매칭 `O(N)`)
*   **공간 복잡도**: `O(M)` (LPS 배열 저장용)
*   **장점**: 최악의 경우에도 선형 시간 복잡도를 보장합니다.
*   **단점**: LPS 배열을 계산하는 전처리 과정이 필요합니다.

```python
def compute_lps_array(pattern):
    """KMP 알고리즘을 위한 LPS(Longest Proper Prefix which is also Suffix) 배열 계산"""
    M = len(pattern)
    lps = [0] * M
    length = 0 # 현재까지 일치하는 접두사-접미사의 길이
    i = 1

    while i < M:
        if pattern[i] == pattern[length]:
            length += 1
            lps[i] = length
            i += 1
        else:
            if length != 0:
                length = lps[length - 1]
            else:
                lps[i] = 0
                i += 1
    return lps

def kmp_search(text, pattern):
    """KMP 문자열 매칭 알고리즘"""
    N = len(text)
    M = len(pattern)
    lps = compute_lps_array(pattern) # LPS 배열 전처리

    i = 0 # 텍스트 인덱스
    j = 0 # 패턴 인덱스
    occurrences = [] # 패턴이 발견된 시작 인덱스 리스트

    while i < N:
        if pattern[j] == text[i]:
            i += 1
            j += 1

        if j == M: # 패턴을 찾았을 경우
            occurrences.append(i - j)
            j = lps[j - 1] # 다음 탐색을 위해 패턴 인덱스 조정
        elif i < N and pattern[j] != text[i]: # 불일치 발생
            if j != 0:
                j = lps[j - 1]
            else:
                i += 1 # 패턴의 첫 문자와도 불일치하면 텍스트 인덱스만 이동
    return occurrences

text_kmp = "ABABDABACDABABCABAB"
pattern_kmp = "ABABCABAB"
print(f"KMP 검색 결과 (텍스트: {text_kmp}, 패턴: {pattern_kmp}): {kmp_search(text_kmp, pattern_kmp)}")
# 예상 출력: [10]
print("
")
```

### 1.3. 라빈-카프 (Rabin-Karp) 알고리즘

라빈-카프 알고리즘은 해싱(Hashing)을 이용하여 문자열을 비교하는 알고리즘입니다. 텍스트의 부분 문자열과 패턴의 해시 값을 비교하여 일치 여부를 빠르게 확인합니다.

*   **핵심 아이디어**: 텍스트의 각 `M` 길이 부분 문자열의 해시 값을 계산하고, 이 해시 값을 패턴의 해시 값과 비교합니다. 해시 값이 일치하면 실제 문자열 비교를 수행하여 **해시 충돌(Hash Collision)**을 확인합니다. **롤링 해시(Rolling Hash)** 기법을 사용하여 텍스트의 다음 부분 문자열의 해시 값을 `O(1)` 시간에 효율적으로 계산합니다.
*   **동작 방식**:
    1.  **전처리 단계**: 패턴 `P`의 해시 값을 계산합니다. 텍스트 `T`의 첫 `M` 길이 부분 문자열의 해시 값을 계산합니다.
    2.  **매칭 단계**: 텍스트의 현재 부분 문자열 해시 값과 패턴의 해시 값을 비교합니다.
        *   해시 값이 일치하면, 실제 문자열 비교를 수행하여 오탐(false positive)을 제거합니다.
        *   해시 값이 일치하지 않으면, 텍스트의 다음 부분 문자열의 해시 값을 롤링 해시를 이용하여 `O(1)` 시간에 계산하고 다음 비교로 넘어갑니다.
*   **시간 복잡도**:
    *   평균적으로 `O(N + M)` (해시 충돌이 적을 경우)
    *   최악의 경우 `O(NM)` (해시 충돌이 빈번하게 발생할 경우, 예를 들어 모든 해시 값이 동일한 경우)
*   **공간 복잡도**: `O(1)` (해시 값만 저장)
*   **장점**:
    *   다중 패턴 매칭에 효율적입니다.
    *   평균적으로 빠른 성능을 보입니다.
*   **단점**:
    *   해시 충돌 가능성이 있으며, 최악의 경우 성능이 저하될 수 있습니다.
    *   적절한 해시 함수와 소수(prime number) 선택이 중요합니다.

```python
def rabin_karp_search(text, pattern, q=101): # q는 큰 소수
    """라빈-카프 문자열 매칭 알고리즘"""
    N = len(text)
    M = len(pattern)
    d = 256 # 아스키 문자 개수 (알파벳 크기)
    
    p_hash = 0 # 패턴의 해시 값
    t_hash = 0 # 텍스트 부분 문자열의 해시 값
    h = 1 # d^(M-1) % q

    # h = d^(M-1) % q 계산
    for i in range(M - 1):
        h = (h * d) % q

    # 패턴과 텍스트 첫 M 길이 부분 문자열의 해시 값 계산
    for i in range(M):
        p_hash = (d * p_hash + ord(pattern[i])) % q
        t_hash = (d * t_hash + ord(text[i])) % q

    occurrences = []

    # 텍스트를 슬라이딩 윈도우 방식으로 탐색
    for i in range(N - M + 1):
        if p_hash == t_hash: # 해시 값이 일치하면
            # 실제 문자열 비교 (해시 충돌 확인)
            if text[i:i+M] == pattern:
                occurrences.append(i)

        # 마지막 윈도우가 아니면 다음 윈도우의 해시 값 계산 (롤링 해시)
        if i < N - M:
            t_hash = (d * (t_hash - ord(text[i]) * h) + ord(text[i + M])) % q
            # 음수 해시 값 방지
            if t_hash < 0:
                t_hash += q
    return occurrences

text_rk = "GEEKSFORGEEKS"
pattern_rk = "GEEK"
print(f"라빈-카프 검색 결과 (텍스트: {text_rk}, 패턴: {pattern_rk}): {rabin_karp_search(text_rk, pattern_rk)}")
# 예상 출력: [0, 
print("
")
```

### 1.4. 보이어-무어 (Boyer-Moore) 알고리즘

보이어-무어 알고리즘은 실제 텍스트 검색에서 매우 효율적인 성능을 보이는 문자열 매칭 알고리즘입니다. 패턴의 끝에서부터 텍스트와 비교를 시작하며, 불일치가 발생했을 때 패턴을 크게 건너뛸 수 있는 두 가지 규칙(Heuristics)을 사용합니다.

*   **핵심 아이디어**:
    1.  **나쁜 문자 규칙 (Bad Character Rule)**: 텍스트(`T`)와 패턴(`P`)을 비교하다 불일치가 발생했을 때, `T`에서 불일치가 발생한 문자(`T[i]`)가 `P`에 어디에 있는지 찾습니다. `P`에서 `T[i]`와 같은 문자를 찾아, 그 위치가 `T[i]`와 정렬되도록 `P`를 오른쪽으로 이동시킵니다. 만약 `P`에 해당 문자가 없다면, `P`를 불일치 지점 너머로 완전히 이동시킬 수 있습니다.
    2.  **좋은 접미사 규칙 (Good Suffix Rule)**: `P`의 접미사 `t`가 `T`와 일치했지만, 그 앞 문자에서 불일치가 발생했다고 가정합니다. 이 경우, `P` 내에서 `t`와 똑같은 다른 부분 문자열을 찾아서 그 위치로 `P`를 이동시킵니다. 만약 `t`와 일치하는 다른 부분 문자열이 `P`에 없다면, `t`의 가장 긴 접두사와 일치하는 `P`의 접두사를 찾아 이동합니다.
*   **동작 방식**: 비교는 패턴의 오른쪽 끝에서 왼쪽으로 진행됩니다. 불일치 발생 시, **나쁜 문자 규칙**과 **좋은 접미사 규칙**이 제안하는 이동 거리 중 더 큰 값을 선택하여 패턴을 이동시킵니다.
*   **시간 복잡도**:
    *   최악의 경우 `O(NM)` (하지만 특정 패턴에서만 발생하며 드뭅니다).
    *   평균적으로 `O(N/M)` 또는 `O(N)`에 가까운 매우 빠른 성능을 보입니다.
*   **장점**: 실제 응용에서 가장 빠른 문자열 매칭 알고리즘 중 하나입니다.
*   **단점**: 좋은 접미사 규칙까지 완전히 구현하는 것은 다소 복잡합니다.

```python
def build_bad_char_table(pattern):
    """보이어-무어 알고리즘을 위한 나쁜 문자 테이블 생성 (패턴에 있는 각 문자의 마지막 위치 저장)"""
    table = {}
    M = len(pattern)
    for i in range(M):
        table[pattern[i]] = i
    return table

def boyer_moore_search(text, pattern):
    """보이어-무어 문자열 매칭 알고리즘 (나쁜 문자 규칙만 사용한 간소화 버전)"""
    N = len(text)
    M = len(pattern)
    if M == 0:
        return []
    
    bad_char_table = build_bad_char_table(pattern)
    occurrences = []
    s = 0  # 텍스트에 대한 패턴의 시작 위치 (shift)

    while s <= N - M:
        j = M - 1
        while j >= 0 and pattern[j] == text[s + j]:
            j -= 1

        if j < 0: # 패턴을 찾은 경우
            occurrences.append(s)
            # 다음 탐색을 위해 패턴 이동. 텍스트의 (s+M) 위치의 문자가 패턴에 없으면 M만큼, 있으면 그 위치에 맞게 이동
            s += (M - bad_char_table.get(text[s + M], -1)) if s + M < N else 1
        else: # 불일치 발생
            # 나쁜 문자 규칙에 따라 패턴 이동
            # text[s+j]는 불일치가 발생한 텍스트의 문자
            # bad_char_table.get(text[s + j], -1)은 패턴에서 그 문자의 마지막 위치
            shift = j - bad_char_table.get(text[s + j], -1)
            s += max(1, shift) # 최소 1칸은 이동하도록 보장
            
    return occurrences

text_bm = "ABAAABCDABAA"
pattern_bm = "ABAA"
print(f"보이어-무어 검색 결과 (텍스트: {text_bm}, 패턴: {pattern_bm}): {boyer_moore_search(text_bm, pattern_bm)}")
# 예상 출력: [0, 8]
print("
")
```

### 1.5. 문자열 매칭 알고리즘 비교

| 알고리즘       | 핵심 아이디어             | 전처리 시간 | 매칭 시간 (최악) | 매칭 시간 (평균) | 공간 복잡도 | 장점                               | 단점                                   |
| :------------- | :------------------------ | :---------- | :--------------- | :--------------- | :---------- | :--------------------------------- | :------------------------------------- |
| **나이브**       | 직접 비교                 | `O(1)`      | `O(NM)`          | `O(NM)`          | `O(1)`      | 구현 간단                          | 비효율적                               |
| **KMP**          | LPS 배열 활용             | `O(M)`      | `O(N)`           | `O(N)`           | `O(M)`      | 최악의 경우에도 선형 시간 보장     | 전처리 필요                            |
| **라빈-카프**    | 롤링 해시                 | `O(M)`      | `O(NM)`          | `O(N+M)`         | `O(1)`      | 다중 패턴 매칭에 유리              | 해시 충돌 가능성, 최악의 경우 성능 저하 |
| **보이어-무어**  | 나쁜 문자/좋은 접미사 규칙 | `O(M + Σ)`  | `O(NM)`          | `O(N/M)`         | `O(Σ)`      | 실제 응용에서 매우 빠름            | 구현 복잡                            |

*Σ는 알파벳(문자 집합)의 크기를 의미합니다.*

## 2. 접미사 배열 (Suffix Array) 및 LCP 배열

접미사 배열(Suffix Array)과 LCP 배열(Longest Common Prefix Array)은 문자열 처리 및 생물 정보학 분야에서 매우 강력한 도구로 사용되는 자료구조입니다. 이들은 문자열 내의 패턴을 효율적으로 찾고, 반복되는 부분 문자열을 분석하는 데 핵심적인 역할을 합니다.

### 2.1. 접미사 배열 (Suffix Array)

*   **개념**: 문자열 `S`의 모든 접미사(Suffix)를 사전순(lexicographical order)으로 정렬한 후, 각 접미사의 시작 인덱스를 저장한 배열입니다. 문자열 `S`의 길이가 `N`이라면, `N`개의 접미사가 존재하며, 접미사 배열 `SA`는 `N`개의 정수로 구성됩니다.
    *   예를 들어, 문자열 "banana"의 접미사는 "banana", "anana", "nana", "ana", "na", "a" 입니다.
    *   이들을 사전순으로 정렬하면 "a", "ana", "anana", "banana", "na", "nana"가 됩니다.
    *   각 접미사의 원래 시작 인덱스를 저장하면 접미사 배열이 됩니다. (0-indexed)
        *   "a" (인덱스 5) -> `SA[0] = 5`
        *   "ana" (인덱스 3) -> `SA[1] = 3`
        *   "anana" (인덱스 1) -> `SA[2] = 1`
        *   "banana" (인덱스 0) -> `SA[3] = 0`
        *   "na" (인덱스 4) -> `SA[4] = 4`
        *   "nana" (인덱스 2) -> `SA[5] = 2`
        *   따라서 `SA = [5, 3, 1, 0, 4, 2]`
*   **구축 알고리즘**:
    *   **나이브(Naive) 방법**: 모든 접미사를 추출하여 정렬하는 방식으로, `O(N^2 log N)`의 시간이 소요됩니다.
    *   **더블링(Doubling) 알고리즘**: 접미사의 길이를 2배씩 늘려가며 정렬하는 방식으로, `O(N log^2 N)` 또는 `O(N log N)`의 시간이 소요됩니다.
    *   **선형 시간 알고리즘**: SA-IS(Suffix Array - Induced Sorting)와 같은 알고리즘은 `O(N)`의 선형 시간에 접미사 배열을 구축할 수 있습니다.
*   **활용**:
    *   **문자열 검색 (패턴 매칭)**: 접미사 배열을 이진 탐색하여 패턴이 텍스트 내에 존재하는지, 존재한다면 어디에 존재하는지 `O(M log N)` 시간에 찾을 수 있습니다.
    *   **최장 반복 부분 문자열 (Longest Repeated Substring)**: LCP 배열과 함께 사용하여 찾을 수 있습니다.
    *   **최장 공통 부분 문자열 (Longest Common Substring, LCS)**: 두 개 이상의 문자열의 LCS를 찾는 데 활용됩니다.

### 2.2. LCP 배열 (Longest Common Prefix Array)

*   **개념**: LCP 배열은 접미사 배열과 함께 사용되는 보조 배열입니다. `LCP[i]`는 접미사 배열 `SA[i-1]`가 가리키는 접미사와 `SA[i]`가 가리키는 접미사 간의 **최장 공통 접두사(Longest Common Prefix)**의 길이를 저장합니다. `LCP[0]`은 정의되지 않거나 0으로 설정됩니다.
    *   위 "banana" 예시에서 `SA = [5, 3, 1, 0, 4, 2]`
        *   `LCP[1]` = LCP("a", "ana") = 1 ("a")
        *   `LCP[2]` = LCP("ana", "anana") = 3 ("ana")
        *   `LCP[3]` = LCP("anana", "banana") = 0
        *   `LCP[4]` = LCP("banana", "na") = 0
        *   `LCP[5]` = LCP("na", "nana") = 2 ("na")
        *   따라서 `LCP = [0, 1, 3, 0, 0, 2]`
*   **구축 알고리즘**: 접미사 배열이 주어진 경우, **Kasai의 알고리즘**을 사용하여 `O(N)`의 선형 시간에 LCP 배열을 구축할 수 있습니다.
*   **활용**:
    *   **최장 반복 부분 문자열**: LCP 배열의 최댓값이 최장 반복 부분 문자열의 길이가 됩니다.
    *   **최장 공통 부분 문자열**: 두 문자열을 합친 후 접미사 배열과 LCP 배열을 구축하여 찾을 수 있습니다.
    *   **서로 다른 부분 문자열의 개수**: `(N*(N+1)/2) - sum(LCP)` 로 계산할 수 있습니다.

### 2.3. 접미사 배열과 LCP 배열의 구현 및 활용

접미사 배열과 LCP 배열은 함께 사용될 때 그 진가를 발휘합니다. 아래는 `O(N log^2 N)` 더블링 알고리즘을 이용한 접미사 배열 구축과 Kasai의 알고리즘을 이용한 LCP 배열 구축 코드입니다.

```python
def build_suffix_array(text):
    """O(N log^2 N) 시간에 접미사 배열을 구축합니다."""
    N = len(text)
    sa = list(range(N))
    # rank[i]: text[i]에서 시작하는 접미사의 초기 순위 (문자 코드)
    rank = [ord(c) for c in text]
    d = 1
    
    while d < N:
        # sa를 (rank[i], rank[i+d]) 쌍으로 정렬
        # i+d가 범위를 벗어나면 -1로 처리하여 가장 작은 순위를 부여
        sa.sort(key=lambda i: (rank[i], rank[i + d] if i + d < N else -1))
        
        # 새로운 rank 계산
        new_rank = [0] * N
        new_rank[sa[0]] = 0
        for i in range(1, N):
            prev, curr = sa[i-1], sa[i]
            r_prev = (rank[prev], rank[prev + d] if prev + d < N else -1)
            r_curr = (rank[curr], rank[curr + d] if curr + d < N else -1)
            # 이전 접미사와의 순위 쌍을 비교하여 같으면 같은 순위, 다르면 1 증가
            if r_prev != r_curr:
                new_rank[curr] = new_rank[prev] + 1
            else:
                new_rank[curr] = new_rank[prev]
        
        rank = new_rank
        # 모든 접미사가 고유하게 정렬되면 종료
        if rank[sa[-1]] == N - 1:
            break
        d *= 2
        
    return sa

def build_lcp_array(text, sa):
    """Kasai의 알고리즘을 사용하여 O(N) 시간에 LCP 배열을 구축합니다."""
    N = len(text)
    rank = [0] * N
    # rank[i]: text[i]에서 시작하는 접미사가 접미사 배열에서 몇 번째인지
    for i in range(N):
        rank[sa[i]] = i
        
    lcp = [0] * N
    k = 0 # 현재 LCP 값 (이전 계산 값을 재활용)
    for i in range(N):
        # 마지막 순위의 접미사는 비교 대상이 없으므로 스킵
        if rank[i] == N - 1:
            k = 0
            continue
        
        # j: 사전순으로 i번째 접미사의 다음 접미사의 시작 인덱스
        j = sa[rank[i] + 1]
        # 공통 접두사 길이 계산
        while i + k < N and j + k < N and text[i+k] == text[j+k]:
            k += 1
        
        lcp[rank[i] + 1] = k
        
        # LCP-k 보조 정리에 따라 k는 최대 1만 감소
        if k > 0:
            k -= 1
            
    return lcp

text_sa = "banana"
suffix_array = build_suffix_array(text_sa)
lcp_array = build_lcp_array(text_sa, suffix_array)
print(f"접미사 배열 (텍스트: {text_sa}): {suffix_array}")
# 예상 출력: [5, 3, 1, 0, 4, 2]
print(f"LCP 배열 (텍스트: {text_sa}): {lcp_array}")
# 예상 출력: [0, 1, 3, 0, 0, 2]
print("
")
```

## 3. 문자열 압축

문자열 압축은 데이터의 크기를 줄여 저장 공간을 절약하고 전송 시간을 단축하기 위한 기법입니다. 이는 데이터의 중복성(redundancy)을 제거하거나 효율적으로 인코딩함으로써 달성됩니다. 압축 방식은 크게 **무손실 압축(Lossless Compression)**과 **손실 압축(Lossy Compression)**으로 나눌 수 있으며, 문자열 압축은 주로 원본 데이터를 완벽하게 복원할 수 있는 무손실 압축에 해당합니다.

### 3.1. 런 길이 인코딩 (Run-Length Encoding, RLE)

런 길이 인코딩은 가장 간단한 압축 방법 중 하나로, 동일한 문자가 연속해서 나타나는 경우, 해당 문자와 반복 횟수로 대체하여 압축합니다.

*   **동작 방식**: "AAABBCDDDD"와 같은 문자열이 있다면, A가 3번, B가 2번, C가 1번, D가 4번 반복됩니다. 이를 "A3B2C1D4"와 같이 표현하여 압축합니다.
*   **장점**: 구현이 매우 간단하고 빠릅니다. 동일한 값이 길게 반복되는 데이터(예: 흑백 이미지, 아이콘 등)에 효과적입니다.
*   **단점**: 반복이 없는 데이터(예: "ABCDE")의 경우 오히려 데이터 크기가 "A1B1C1D1E1"로 늘어날 수 있습니다.

```python
def rle_encode(text):
    """런 길이 인코딩을 수행합니다."""
    if not text:
        return ""
    
    encoded = []
    count = 1
    for i in range(1, len(text)):
        if text[i] == text[i-1]:
            count += 1
        else:
            encoded.append(text[i-1] + str(count))
            count = 1
    encoded.append(text[-1] + str(count))
    return "".join(encoded)

def rle_decode(encoded_text):
    """런 길이 인코딩된 텍스트를 디코딩합니다."""
    decoded = []
    i = 0
    while i < len(encoded_text):
        char = encoded_text[i]
        i += 1
        num_str = ""
        while i < len(encoded_text) and encoded_text[i].isdigit():
            num_str += encoded_text[i]
            i += 1
        decoded.append(char * int(num_str))
    return "".join(decoded)

text_rle = "AAABBCDDDD"
encoded_rle = rle_encode(text_rle)
decoded_rle = rle_decode(encoded_rle)
print(f"RLE 원본: {text_rle}")
print(f"RLE 인코딩: {encoded_rle}")
print(f"RLE 디코딩: {decoded_rle}")
print("
")
```

### 3.2. 허프만 코딩 (Huffman Coding)

허프만 코딩은 데이터에 나타나는 문자의 빈도수를 기반으로 가변 길이 코드를 할당하는 **통계적 무손실 압축 알고리즘**입니다. 빈도수가 높은 문자에는 짧은 코드를, 빈도수가 낮은 문자에는 긴 코드를 할당하여 전체 데이터의 평균 코드 길이를 최소화합니다.

*   **핵심 아이디어**: 가장 빈번하게 나타나는 문자에 가장 짧은 코드를 부여하고, 가장 적게 나타나는 문자에 가장 긴 코드를 부여하는 **탐욕(Greedy) 알고리즘**입니다. 이를 위해 이진 트리를 구성합니다.
*   **알고리즘 단계**:
    1.  **빈도수 계산**: 입력 문자열에 나타나는 각 문자의 빈도수를 계산합니다.
    2.  **리프 노드 생성**: 각 문자를 하나의 리프 노드로 만들고, 해당 문자의 빈도수를 노드의 가중치로 설정합니다. 이 노드들을 **최소 힙(Min-Priority Queue)**에 삽입합니다.
    3.  **트리 구축**:
        *   힙에서 가중치가 가장 작은 두 개의 노드를 추출합니다.
        *   이 두 노드를 자식으로 하는 새로운 내부 노드를 생성합니다. 새로운 내부 노드의 가중치는 두 자식 노드의 가중치 합으로 설정합니다.
        *   새로운 내부 노드를 다시 힙에 삽입합니다.
        *   힙에 하나의 노드(루트 노드)만 남을 때까지 이 과정을 반복합니다.
    4.  **코드 할당**: 구축된 허프만 트리에서 루트부터 리프 노드까지 순회하면서 코드를 할당합니다. 일반적으로 왼쪽 자식으로 이동할 때는 '0', 오른쪽 자식으로 이동할 때는 '1'을 부여합니다. 이렇게 생성된 코드는 **접두사 코드(Prefix Code)** 속성을 가집니다. (어떤 문자의 코드도 다른 문자의 코드의 접두사가 되지 않습니다.)
*   **특징**:
    *   **최적성**: 문자 단위 인코딩에서는 이론적으로 가장 효율적인 압축 방법 중 하나입니다.
    *   **가변 길이 코드**: 문자마다 길이가 다른 코드를 사용합니다.
    *   **무손실 압축**: 원본 데이터를 완벽하게 복원할 수 있습니다.
*   **시간 복잡도**: `O(N log N)` (N은 고유 문자의 개수). 힙 연산에 의해 결정됩니다.
*   **공간 복잡도**: `O(N)` (허프만 트리 및 코드 저장용).

```python
import heapq
from collections import Counter

class HuffmanNode:
    def __init__(self, char, freq):
        self.char = char
        self.freq = freq
        self.left = None
        self.right = None

    # 우선순위 큐에서 비교를 위한 메서드
    def __lt__(self, other):
        return self.freq < other.freq

def build_huffman_tree(text):
    """텍스트로부터 허프만 트리를 구축합니다."""
    frequency = Counter(text) # 각 문자의 빈도수 계산
    priority_queue = []

    # 각 문자를 리프 노드로 만들어 우선순위 큐에 삽입
    for char, freq in frequency.items():
        heapq.heappush(priority_queue, HuffmanNode(char, freq))

    # 힙에 하나의 노드만 남을 때까지 트리 구축
    while len(priority_queue) > 1:
        node1 = heapq.heappop(priority_queue)
        node2 = heapq.heappop(priority_queue)

        # 두 노드를 합쳐 새로운 내부 노드 생성
        merged_node = HuffmanNode(None, node1.freq + node2.freq)
        merged_node.left = node1
        merged_node.right = node2
        heapq.heappush(priority_queue, merged_node)

    return priority_queue[0] if priority_queue else None # 루트 노드 반환

def generate_huffman_codes(node, current_code, codes):
    """허프만 트리로부터 각 문자의 코드를 생성합니다."""
    if node is None:
        return

    if node.char is not None: # 리프 노드인 경우 (문자를 가짐)
        codes[node.char] = current_code if current_code else "0"
        return

    # 왼쪽 자식으로 이동 (0 추가)
    generate_huffman_codes(node.left, current_code + "0", codes)
    # 오른쪽 자식으로 이동 (1 추가)
    generate_huffman_codes(node.right, current_code + "1", codes)

def huffman_encode(text):
    """허프만 코딩을 사용하여 텍스트를 인코딩합니다."""
    if not text:
        return "", {}

    root = build_huffman_tree(text)
    codes = {}
    generate_huffman_codes(root, "", codes)

    # 텍스트에 문자가 하나만 있는 경우 처리
    if len(codes) == 1:
        char = list(codes.keys())[0]
        codes[char] = "0"

    encoded_text = "".join(codes[char] for char in text)
    return encoded_text, codes

def huffman_decode(encoded_text, codes):
    """허프만 코딩된 텍스트를 디코딩합니다."""
    if not encoded_text:
        return ""

    # 코드 딕셔너리를 역으로 (코드 -> 문자) 변환
    reverse_codes = {code: char for char, code in codes.items()}
    
    # 텍스트에 문자가 하나만 있는 경우 처리
    if not reverse_codes:
        char, code = list(codes.items())[0]
        return char * len(encoded_text)

    decoded_text = []
    current_code = ""
    for bit in encoded_text:
        current_code += bit
        if current_code in reverse_codes:
            decoded_text.append(reverse_codes[current_code])
            current_code = "" # 코드 매칭 후 초기화
    return "".join(decoded_text)

text_huffman = "this is an example for huffman encoding"
encoded_text, codes = huffman_encode(text_huffman)
print(f"원본 텍스트: {text_huffman}")
print(f"인코딩된 텍스트: {encoded_text}")
print(f"허프만 코드: {codes}")
decoded_text = huffman_decode(encoded_text, codes)
print(f"디코딩된 텍스트: {decoded_text}")
print("
")
```

### 3.3. 다른 문자열 압축 알고리즘

*   **LZ77 및 LZ78 (Lempel-Ziv family)**: **사전 기반 압축(Dictionary-based Compression)** 알고리즘의 기초가 됩니다. 이미 나타난 문자열 패턴을 사전(dictionary)에 저장하고, 반복되는 패턴을 사전의 인덱스로 대체하여 압축합니다.
    *   **LZW (Lempel-Ziv-Welch)**: LZ78의 변형으로, GIF, TIFF, PDF 파일 형식 등에서 널리 사용됩니다.
*   **산술 코딩 (Arithmetic Coding)**: 허프만 코딩보다 더 높은 압축률을 제공할 수 있는 통계적 압축 방법입니다. 전체 메시지를 0과 1 사이의 하나의 소수(fraction)로 인코딩합니다. 구현이 복잡합니다.

### 3.4. 문자열 압축의 활용

*   **파일 압축**: ZIP, GZIP, RAR 등 다양한 파일 압축 형식의 핵심 기술입니다.
*   **데이터 전송**: 네트워크를 통해 데이터를 전송할 때 대역폭을 절약하기 위해 사용됩니다.
*   **데이터베이스**: 저장 공간을 효율적으로 사용하기 위해 데이터베이스 내의 텍스트 데이터를 압축합니다.
*   **멀티미디어**: 이미지(JPEG, PNG), 오디오(MP3), 비디오(MPEG) 등 멀티미디어 데이터 압축에 활용됩니다.
